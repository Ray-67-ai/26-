# 2026 电赛 H 题第三问：Codex 完整交接说明

更新时间：2026-08-01  
用途：当前 Codex 额度将用完，把本文件交给另一个 Codex 账号后，可直接继续第三问开发、烧录、实车验证和后续优化。

---

## 0. 可直接复制给新 Codex 的接手提示词

> 请先完整阅读 `C:\Users\ASUSlxg\Documents\Codex\2026-07-29\xan\2026电赛H题第三问_Codex完整交接.md`，把它作为已经确认的项目上下文。不要重新采用 D36 方案，不要覆盖已调好的第二问，不要把临时自动调参固件当成正式固件。当前第三问已经在实物上完成视觉闭环和 ZDT 电机调参，正式工程也已写入最终参数并编译成功，但正式 `.out` 尚未烧录到板子。请先检查文件和工作区现状，再根据我的新问题继续；遇到未确认的机械尺寸、实际引脚或运动异常时向我提问，不要私自假设。

---

## 1. 题目和当前目标

- 题目：2026 电赛 H 题《车载平衡滚球运动控制系统》。
- 题目文件：`F:\Ti_1\26\2026电赛命题（TI杯赛区）\H题_车载平衡滚球运动控制系统.docx`
- 第二问巡线小车已经调好。
- 第三问：控制水管倾角，使小球先到正五厘米附近，再运动到负五厘米并停稳。
- 当前实际目标坐标：
  - 起始：`0 mm`
  - 第一目标：`+50 mm`
  - 最终目标：`-50 mm`
- 当前程序在位置达到 `+45 mm` 时提前切换到负目标，利用惯性使正端峰值到达约 `+48 mm`。
- 最终完成判定：
  - `|-50 - x| <= 5 mm`
  - `|速度| <= 25 mm/s`
  - 连续稳定 `400 ms`
  - 总时间限制 `5000 ms`

---

## 2. 已确认硬件，禁止回退到旧方案

### 主控

- 立创·天猛星 MSPM0G3507 开发板及扩展板。

### 第三问步进电机

- 实际使用：ZDT X42S 第二代闭环步进电机。
- 接口：TTL/UART。
- 固件：Emm。
- 不是 D36，D36 方案已经废弃。
- 不是 RS485，不要使用 Modbus/RS485 接法。
- 电机 12V 独立供电。
- 电机已做过初始化/校准和零位设置，不要无故重新校准、恢复出厂或重新清零。

ZDT 资料：

- `F:\Ti_1\AA电机资料\ZDT_XS系列第二代闭环步进电机资料\3. 说明书\ZDT_X42S第二代闭环步进电机用户手册V1.0.5_260527.pdf`
- `F:\Ti_1\AA电机资料\ZDT_XS系列第二代闭环步进电机资料\3. 说明书\ZDT闭环步进电机MODBUS协议使用说明V1.0.1_260401.pdf`

注意：第二份 Modbus 文件仅作为资料保留，当前电机实际走 TTL/UART。

### 视觉

- MaixCAM Pro 持续识别小球位置。
- MaixCAM 通过 UART 把位置数据发送给天猛星。
- GoPro 仅用于图像拍摄/传输，不直接连接 MSPM0 做闭环数据源。

### ICM45686

- 计划硬件 I2C。
- `AD0 -> GND`。
- 参考引脚：`I2C1 PB3/SDA、PB2/SCL`。
- SPI 不用。
- ICM 建议安装在水管尾部、靠近步进电机连接处的刚性位置，避开小球滚动路径。
- 当前这次成功的 PID 调试没有把 ICM 数据加入闭环；目前依靠 MaixCAM 位置/速度和 ZDT 内部编码器。

---

## 3. 已确认接线

### MaixCAM Pro 到天猛星

| MaixCAM | 天猛星 | 说明 |
|---|---|---|
| A19 TX | PA9 / UART1_RX | 摄像头向 MCU 发送位置 |
| A18 RX | PA8 / UART1_TX | MCU 向摄像头回传/应答 |
| GND | GND | 必须共地 |

### ZDT X42S TTL 到天猛星

| ZDT 接口 | 天猛星 | 说明 |
|---|---|---|
| R/A/H（TTL 模式为 RX） | PA10 / UART0_TX | MCU 发命令给电机 |
| T/B/L（TTL 模式为 TX） | PA11 / UART0_RX | 电机应答给 MCU |
| GND | GND | 必须共地 |
| V+ | 12V 电源 | 独立电机供电 |

禁止事项：

- CH340 的 TX/RX 不能和 PA10/PA11、电机同时并接。
- CH340 的 5V/3.3V 不要接到电机。
- J-Link SWD 可以一直保留连接。
- UART 模式下 COM、EN、STP、DIR 暂不接。
- 不要热插拔电机线。

ZDT 当前通信参数：

- 波特率 `115200`
- 地址 `1`
- `8N1`
- 校验字节 `0x6B`
- 通信端口 `UART_FUN`
- 控制命令应答 `Receive`

成功的位置指令应答曾反复收到：

```text
01 FD 02 6B
```

---

## 4. 正式工程和临时调参工程必须区分

### 正式工程

```text
F:\ti\26H\H2_LineCar_CCS
```

正式输出文件：

```text
F:\ti\26H\H2_LineCar_CCS\Debug\H2_LineCar_CCS.out
```

该文件在 2026-08-01 19:09 左右重新编译成功，大小约 `269688` 字节。

正式工程保留第二问/第三问运行选择，不包含 RTT 自动调参逻辑。

### 临时自动调参工程

```text
C:\Users\ASUSlxg\Documents\Codex\2026-07-29\xan\H3_Autotune_Temp
```

临时输出：

```text
C:\Users\ASUSlxg\Documents\Codex\2026-07-29\xan\H3_Autotune_Temp\AutotuneBuild\H3_Autotune_Temp.out
```

临时工程特征：

- `H3_AUTOTUNE_BUILD=1`
- 物理 Q3 被忽略。
- 只能通过 J-Link RTT 的 ARM/START/STOP 等命令运动。
- 有实时遥测、运行时修改 PID、角度限制和 CSV 日志。
- 仅用于台架调参，不是最终比赛固件。

重要：现在 MCU 板子里仍然是之前烧录的临时调参固件；正式 `.out` 已经生成，但尚未替用户烧录。下一步应烧录正式 `H2_LineCar_CCS.out`，不要再复制或烧录 `H3_Autotune_Temp.out`。

---

## 5. 最终正式参数

正式文件：

```text
F:\ti\26H\H2_LineCar_CCS\src\h3_config.h
```

最终参数：

```c
#define H3_BALL_KP_DEG_PER_MM          (0.1600f)
#define H3_BALL_KI_DEG_PER_MM_S        (0.0030f)
#define H3_BALL_KD_DEG_PER_MM_S        (0.1000f)
#define H3_MAX_MOTOR_ANGLE_DEG          (5.5f)
#define H3_MAX_MOTOR_SLEW_DEG_S         (100.0f)
#define H3_ZDT_CW_RAISES_RIGHT          (1)
```

参数含义：

- `Kp` 单位近似为电机度/mm。
- `Ki` 单位近似为电机度/(mm·s)。
- `Kd` 单位近似为电机度/(mm/s)。
- `5.5°` 是 ZDT 电机轴目标角，不一定等于水管真实倾角；连杆孔位和长度会改变比例。

控制公式：

```text
error = target_position - measured_position
motor_angle = -Kp*error - Ki*integral + Kd*ball_velocity
```

积分只在负目标/最终保持阶段启用；去正五厘米时积分清零，防止正向积累导致切换后严重过冲。

---

## 6. 已写入正式工程的代码修改

正式源文件仅改动了以下三个第三问相关文件：

1. `src/h3_config.h`
   - 写入最终 PID、最大角度、角度变化速度。

2. `src/zdt_stepper.c`
   - UART 改成逐字节阻塞发送。
   - 每个 ZDT 命令帧后增加 `2 ms` 帧间隔。
   - 解决连续命令拼帧后电机返回 `01 00 EE 6B` 的问题。

3. `src/h3_ball_control.c`
   - `DONE` 或 `TIMEOUT` 后立即把目标清零并命令电机回机械零角度。
   - 修复超时后继续计算上一个负目标、又发出错误反向角度的问题。

第二问控制源代码未被覆盖。

正式工程编译命令：

```powershell
F:\ti\ccs2051\ccs\utils\bin\gmake.exe -C F:\ti\26H\H2_LineCar_CCS\Debug -j 4 all
```

2026-08-01 已执行成功，无编译/链接错误。

正式工程工作树里在此次修改前就已有 Debug 输出变化和临时 `.out` 文件。不要使用 `git reset --hard` 或删除用户文件。

---

## 7. 实物调参结果

最终参数连续三次进入 `DONE`：

| 试验日志 | 起点 | 正端峰值 | 到正端时间 | 负端最低点 | DONE 时间 | DONE 位置 | DONE 速度 |
|---|---:|---:|---:|---:|---:|---:|---:|
| `h3_trial_20260801_185345.csv` | +7 mm | +47.0 mm | 1.719 s | -62.0 mm | 4.860 s | -48.0 mm | 0.8 mm/s |
| `h3_trial_20260801_185919.csv` | +1 mm | +48.0 mm | 1.656 s | -60.0 mm | 4.859 s | -51.0 mm | 2.6 mm/s |
| `h3_trial_20260801_190451.csv` | +5 mm | +47.9 mm | 1.782 s | -61.2 mm | 4.891 s | -48.3 mm | 14.1 mm/s |

日志目录：

```text
C:\Users\ASUSlxg\Documents\Codex\2026-07-29\xan\H3_Autotune_Temp\logs
```

这三轮是当前最终参数的主要证据。

失败/对比试验：

- `Kp=0.14, Ki=0.005, Kd=0.07, max=5.5°`
  - 能完成正负运动，但负端冲到约 `-69.9 mm`。
  - 5 秒时仍有明显速度，没有停稳。
- `Kp=0.18, Ki=0.003, Kd=0.10, max=6°`
  - 正向约 1.41 秒到达，更快。
  - 负端过冲约 `-68 mm`，5 秒内没有稳定完成。
- `Kp=0.16, Ki=0.003, Kd=0.105`
  - D 项过强，正向阶段被压得过慢。
  - 快到 5 秒才完成正端切换，最终失败。

所以最终不是选择更大的 Kp、Kd，而是选择速度和停稳之间的折中：`0.16 / 0.003 / 0.10`。

---

## 8. 调参思路和物理结论

系统结构：

```text
MaixCAM 小球位置
        -> 位置/速度滤波
        -> 状态机产生 +50 或 -50 mm 目标
        -> 外环 PID 计算电机目标角度
        -> 角度限幅和角速度限制
        -> ZDT 内部编码器闭环执行绝对角度
        -> 连杆带动水管
        -> 小球滚动
        -> MaixCAM 再次测量
```

实际发现：

- 机构有明显死区和左右不对称。
- 约 `1°、2°、3°` 经常不足以让小球克服静摩擦和连杆回差。
- 大约 `4～5.5°` 才能可靠起动，具体方向不同。
- 一次看到 2° 后移动几毫米不能证明控制方向，可能只是停机后的机构释放。
- 球在水管端部会真实卡住；曾到 `+115 mm` 后需要用户手动拨开。PID 无法解决机械卡死。
- 停机后如果直接禁能且水管仍倾斜，小球会继续滚动。因此临时固件后来改为 STOP 后回零并保持电机力矩。

PID作用：

- P：根据位置误差决定主要倾角，负责推动和克服死区。
- D：根据小球速度提前反向倾斜刹车，控制过冲。
- I：只用很小数值补偿水管不水平、摩擦和安装偏差，过大会导致积分饱和和大过冲。

推荐后续调参顺序：

1. 确认视觉坐标方向、单位和零点。
2. 单独测试两个方向的电机角度与球运动方向。
3. 测出左右方向能可靠起动的最小角度。
4. Ki 先设 0，先调 Kp 和最大角度，使球能完成全程。
5. 增加 Kd 抑制负端过冲。
6. 最后加很小 Ki 消除长期位置偏差。
7. 每次只改一个量，记录位置、速度、角度和状态切换时间。
8. 至少连续三次满足位置、速度和时间再确认参数。

典型现象判断：

| 现象 | 优先处理 |
|---|---|
| 电机动但球不动 | 查死区/卡滞，提高起动角或做死区补偿 |
| 正向太慢 | 增加 Kp/最大角度，或减小 D |
| 正端冲太远 | 提前切换、增加 D、减小最大角度 |
| 负端过冲大 | 增加 D，或降低 Kp/最大角度 |
| 最终长期偏离 -50 | 小幅增加 Ki，或做机械零偏补偿 |
| 指令抖动 | 减小 D、加强速度滤波、提高最小角度变化阈值 |

---

## 9. 临时 RTT 自动调参工具

主机控制脚本：

```text
C:\Users\ASUSlxg\Documents\Codex\2026-07-29\xan\H3_Autotune_Temp\tools\h3_rtt_control.py
```

构建脚本：

```text
C:\Users\ASUSlxg\Documents\Codex\2026-07-29\xan\H3_Autotune_Temp\tools\build_autotune.ps1
```

J-Link：

- 序列号：`20090928`
- 设备：Cortex-M0
- SWD 速度：1000 kHz
- RTT 控制块地址：`0x20200978`
- RTT TCP：`127.0.0.1:19021`
- SEGGER：`F:\Program Files (x86)\SEGGER\JLink_V614e\JLink.exe`

临时脚本可用命令：

```powershell
python tools\h3_rtt_control.py status --seconds 1.5
python tools\h3_rtt_control.py trial --kp 0.16 --ki 0.003 --kd 0.10 --max-angle 5.5 --slew 100 --duration 7 --position-guard 130
python tools\h3_rtt_control.py center --kp 0.20 --ki 0.02 --kd 0.12 --max-angle 8 --slew 120 --duration 10 --position-guard 130
```

临时调参协议命令：

```text
ARM
START
RESET
STOP
STATUS
SET,kp乘1e5,ki乘1e5,kd乘1e5
LIMIT,角度毫度,角速度毫度每秒
RAWTEST
RAWBACK
```

遥测字段：

```text
T,MCU时间,状态,armed,位置x10,速度x10,目标x10,角度x1000,
  Kpx1e5,Kix1e5,Kdx1e5,参数代数,valid,elapsed,
  ZDT发送帧,ZDT接收帧,ZDT成功,E2错误,EE错误,最后功能码,最后状态
```

已知原始测试命令：

```text
01 FD 01 00 1E 32 00 00 00 2C 02 00 6B
```

- 相对位置命令，约 5°。
- 每发送一次都会继续移动约 5°。
- 严禁周期发送。

临时调参护栏源代码后来已放宽到：

- `Kp <= 0.50`
- `Ki <= 0.20`
- `Kd <= 0.50`
- 角度 `<= 15°`
- 角速度 `<= 720°/s`

注意：放宽后的源代码尚未重新烧进当前 MCU；当前板内仍是之前烧录的临时版本。正式比赛不需要这些运行时调参范围。

---

## 10. 正式程序的操作方式

正式 `main.c` 当前保留运行时题目选择：

1. 上电或 RESET 后进入题目选择菜单。
2. 默认选择 H2。
3. Q1 在 H2/H3 之间切换。
4. Q3 启动选中的题目。
5. H3 启动时先清理视觉 UART 旧数据。
6. 收到有效视觉帧后进入第三问控制。

用户在自动调参期间提出“不按 Q3，由 Codex 自动启动”，这个要求只用于临时 RTT 固件。正式程序按用户要求保持原来的按键/模式结构，没有改成上电自动运行。

---

## 11. 下一位 Codex 接手后的优先事项

1. 不要再重新调电机通信，TTL 接线和 ZDT 指令已经实物验证成功。
2. 先确认正式源文件中的最终参数和三处修复仍在。
3. 烧录正式文件：
   - `F:\ti\26H\H2_LineCar_CCS\Debug\H2_LineCar_CCS.out`
4. 烧录后做一次静态正式程序验证：
   - 12V 电机电源打开。
   - 小球放在 0 刻度，确认没有卡在端头。
   - MaixCAM 数据正常。
   - 选择 H3 并启动。
   - 观察是否仍能在约 4.86～4.89 秒进入 DONE。
5. 再做车辆运动状态验证。车体加减速、振动和水管安装变化可能需要小幅修正参数。
6. 若车体运动时偏差明显，可考虑使用 ICM45686 做车体姿态/加速度前馈；当前成功版本尚未集成该前馈。
7. 不要未经用户确认修改摇臂孔位、机械零位、摄像头标定、正式启动方式或第二问巡线控制。

---

## 12. 关键安全和工作原则

- 电机调试时 12V 可以保持开启；没有 12V 电机无法带动水管。
- 烧录本身不要求关闭 12V，但临时固件启动默认不应主动运动，正式固件初始化会使能 ZDT 并发送零角命令。
- 视觉无效、串口遥测丢失、位置明显越界时应停止试验。
- 调参保护可以灵活放宽，但不能删除通信丢失、视觉失效和极端越界保护。
- 机械端部卡球是物理故障，需要先拨开或改结构，不能继续靠增加 PID 解决。
- 不要声称水管真实倾角就是电机命令角度；中间有连杆传动比和间隙。
- 不要使用 `git reset --hard`、`git checkout --` 等覆盖用户现有修改。

---

## 13. 一句话当前状态

第三问在临时 RTT 固件下已经用 `Kp=0.16、Ki=0.003、Kd=0.10、最大5.5°、100°/s` 连续三次实物完成；正式工程已移植参数、ZDT 帧间隔修复和超时回零修复并编译出正式 `.out`，下一步是烧录正式 `.out` 后验证正式按键流程和车辆运动状态。
