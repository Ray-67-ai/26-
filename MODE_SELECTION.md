# H2/H3/H4/H5 比赛模式选择

本工程只需烧录一个正式固件，通过扩展板按键选择第二问至第五问。

## 按键分配

| 按键 | 引脚 | 功能 |
|---|---|---|
| Q1 | PA23 | 循环选择 `H2 -> H3 -> H4 -> H5 -> H2`，仅选择、不启动 |
| Q2 | PA21 | 停用（该按键此前疑似损坏） |
| Q3 | PB18 | 启动 OLED 当前显示的题目 |
| Q4 | PA17 | 预留 |

## 比赛操作

1. 上电或按硬件 RESET，OLED 进入模式菜单，默认显示 `SELECT: H2 LINE`。
2. 第二问无需切换，直接按 Q3。
3. 第三问按 Q1 一次，确认 OLED 显示 `SELECT: H3 BALL`，再按 Q3。
4. 第四问按 Q1 两次，确认 OLED 显示 `SELECT: H4 AB`，人工把球放在零刻度附近，再按 Q3。
5. 第五问按 Q1 三次，确认 OLED 显示 `SELECT: H5 LAP`，人工把球放在零刻度附近，再按 Q3。
6. H4/H5 收到有效视觉数据后闭合球位置控制；球连续处于 `±3 mm`、速度不超过 `8 mm/s` 达 `300 ms` 后，小车开始运行。
7. 题目启动后模式锁定，Q1 不再切换，避免运行中误操作。更换题目必须按硬件 RESET 回到菜单。

## H5正式固化参数

- 视觉球控：Kp=0.075、Ki=0、Kd=0.045、预测20 ms。
- 巡航速度：140 rpm；启动平滑加速时间：2700 ms。
- 防卡短脉冲：巡航阶段5°。
- 第一阻尼窗口：1.70–3.20 m，Kd×1.5。
- 第二阻尼窗口：4.70–5.70 m，Kd×1.5。
- 第二次检测到A点后平滑减速，约再行驶0.46–0.50 m停车。
- 未检测到A点的安全超时为60 s。

正式第四问沿用实车选定的 `020741` 控制参数，只把按键后的零点稳定等待由 400 ms 调整为 300 ms。程序不使用 ICM/陀螺仪。
