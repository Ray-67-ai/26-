# H题第三问调试说明

## 代码分区

- `src/vehicle_config.h`、`src/app.c`：第二问，第三问不修改这里的参数。
- `src/h3_config.h`：第三问全部可调参数。
- `src/h3_vision.c`：接收 MaixCAM 坐标并由相邻帧计算速度。
- `src/zdt_stepper.c`：ZDT Emm TTL 普通绝对位置命令。
- `src/h3_ball_control.c`：第三问状态机和球位置外环。
- `src/competition_mode.h`：运行时题目编号定义；无需重新编译切换题目。
- `maixcam/h3_ball_sender.py`：MaixCAM Pro 识别和发送坐标。

## 串口帧

MaixCAM 每个有效图像帧发送一行：

```text
B,sequence,camera_ms,position_mm,valid\n
```

例如钢球位于 +4.8 cm：

```text
B,135,4821,48,1
```

识别失败时仍发送一帧，但 `valid=0`：

```text
B,136,4854,0,0
```

## 第一次上电调试顺序

1. 不放钢球，断开连杆，只验证 ZDT 能响应使能和 0 度命令。
2. 接连杆但不放球，将 `H3_MAX_MOTOR_ANGLE_DEG` 临时改为 `1.0f`，确认正角度确实让右端升高。
3. MaixCAM 单独运行，先调整 `BALL_ROI`、两个管端像素坐标和 `BALL_THRESHOLDS`，确保 OLED 的 X 坐标方向、数值正确。
4. 水管断电调平，球放 O 点，上电后确认电机显示 0 度。
5. 把 `H3_BALL_KI_DEG_PER_MM_S` 暂时改为 `0.0f`，只调 Kp：能从 O 点平稳到 +5 cm。
6. 再增加 Kd，让折返和 -5 cm 刹车不过冲。
7. 最后恢复很小的 Ki，只消除 -5 cm 附近的静差。
8. 确认稳定后再逐步增大最大角度和响应速度，禁止一开始直接使用大角度。

## 运行顺序

1. 断电时人工把水管调平，钢球放 O 点。
2. 上电或按硬件 RESET，OLED 出现题目选择菜单，默认选中 H2。
3. 按 Q1 把 `SELECT` 切换到 H3，再按 Q3 启动；程序清空视觉串口旧数据并等待 MaixCAM 的第一帧有效坐标。
4. 收到有效坐标后第三问自动开始，执行 O -> +5 cm -> -5 cm并稳定。
5. 本次运行中题目会锁定；若要改选第二问，按硬件 RESET 回菜单，选择 H2 后按 Q3。
6. `DONE` 表示在 -5 cm ±5 mm 且速度小于 25 mm/s 连续保持 400 ms。
7. 第三问运行中再次按 Q3 可按原有第三问按键逻辑复位/重启。
 # 反转更快
让水管翻转更快 ，是你说的快到 +5 cm 时“快速抖一下、迅速反向”。
//#define H3_MAX_MOTOR_SLEW_DEG_S (90.0f)
表示软件只允许电机目标角度每秒变化 90°。
例如水管从 -4° 翻到 +6°：
角度变化 = 10°
所需时间约 = 10 / 90 = 111 ms
改为：
#define H3_MAX_MOTOR_SLEW_DEG_S (180.0f)
这样 -4° → +6° 理论上只需要约：
10 / 180 = 56 ms
这就是你说的快到 +5 cm 时“快速抖一下、迅速反向”。
# 运动速度
提高步进电机运动速度
#define H3_ZDT_SPEED_RPM    (120U)
#define H3_ZDT_ACCELERATION (80U)
H3_ZDT_SPEED_RPM：步进电机转动速度。
H3_ZDT_ACCELERATION：电机从静止升到目标速度、以及反向时的加速能力。
速度高但加速度低：电机还是会慢慢起步。
加速度高但速度低：起步快，但最终转动速度受限。
第一次先用 180 / 120，不要直接把数值调得特别大，否则连杆可能冲击、步进电机可能丢步。

# KP
#define H3_BALL_KP_DEG_PER_MM (0.0800f)
位置差每增加 1 mm，增加约 0.08° 的倾斜命令。
Kp 增大：从 O 点出发更快，到 -5 cm 也更快。
Kp 太小：反应慢，可能超时。
Kp 太大：球会冲过目标，左右来回摆。
如果改完虚拟目标后，球从 O 到 +5 仍然慢，可以逐步改：
0.080 → 0.090 → 0.100
一次只增加 0.01。
# KD
#define H3_BALL_KD_DEG_PER_MM_S (0.0100f) 
Kd 根据球的速度进行提前制动
当球向正方向快速移动时，velocity 为正，Kd 会产生正角度，帮助水管提前反向制动；当球快速冲向 -5 cm 时，Kd 同样会阻止它冲过 -5 cm。
Kd 增大：制动更早，最终不容易过冲。
Kd 太大：球还没到 +5 cm 就明显减速，反而拖长时间。
Kd 太小：球冲过 +5 或 -5，来回摆很久。

#   KI
H3_BALL_KI_DEG_PER_MM_S
Ki 用来消除最终位置静差。
例如球最后一直停在 -46 mm，差 4 mm，Kp 输出可能不足以克服摩擦，Ki 会逐渐累积，帮助球继续移动到 -50 mm。
但是 Ki 太大会导致：
已经靠近 -5 cm，水管还保持较大角度；
球冲过 -5 cm；
来回晃动；
需要更长时间才能满足稳定判定。
所以调试顺序是：先用 0.0000f
Kp、Kd 调好以后，球始终差几毫米，再加：0.003 → 0.005 → 0.008.不建议一开始就用当前的 0.0100。
